const a = 10;
const c = 10;
const b = 10;

/**
 * sdfasdf
 * @param {Function} fn - asdf
 * @returns {Function} - asdf
 */
function test(fn) {
  return fn;
}

const name = 'asdf';

function random() {
  return 10;
}

console.log(a, b, c, name, test(() => ({
  name: 'adsf',
})));
