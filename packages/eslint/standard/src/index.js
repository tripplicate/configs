// const { isPackageExists } = require('local-pkg');

// const hasTypescript = isPackageExists('typescript');

module.exports = {
  extends: [
    './configs/typescript',
    './configs/vue',
    './configs/documents'
  ],
};
